{
  "name": "@kz/server",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Kanzasset çekirdeği (demo): AMR bağlantı katmanı, fiyat motoru (marj + komisyon), müşteri işlemleri açık / durdu kuralı, K ekranları için API ve canlı akış.",
  "scripts": {
    "dev": "NODE_OPTIONS=--no-warnings tsx watch src/index.ts",
    "start": "NODE_OPTIONS=--no-warnings tsx src/index.ts",
    "test": "NODE_OPTIONS=--no-warnings tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@amr/contract": "*",
    "@fastify/cors": "^10.0.2",
    "@fastify/static": "^8.1.0",
    "fastify": "^5.2.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
